# Python 3.11+ (tomllib er indbygget). Testet på 3.14.
#
# Eksterne værktøjer (IKKE pip — se README):
#   - ScanTailor Advanced  (split + deskew + dewarp; scantailor-cli)
#
# Python-pakker:
easyocr           # OCR (dansk+engelsk); henter sprogmodeller ved 1. kørsel
edge-tts          # tekst -> MP3 (Microsoft neurale stemmer, gratis)
wordfreq          # dansk ordliste til volapyk-filter
mistralai         # Mistral OCR API (hovedvejen; kræver MISTRAL_API_KEY)
python-dotenv     # indlæser ~/ocr_project/.env med API-nøglen
Pillow            # billedindlæsning + hvidpunkt-klip (følger typisk med easyocr)
pymupdf           # søgbar PDF: usynligt lag + verificering
python-doctr      # linjedetektion til søgbart lag, db_resnet50 (testet: 1.1.0)
numpy             # (følger typisk med doctr/easyocr)
